telefonu döndürünce activitydeki stateler gidiyor mesela kullanýcýnýn ne kadar butona bastýðý yada kayýtlý olan loglar

        <activity android:name=".MainActivity" android:screenOrientation="">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

Bundle: arkada bir dosyaya yazýyor key-value pairlerini tutuyor. 

uygulama destroy olduðunda tüm bundlelar gidiyor.

commit() hepsini birden yaz, tek tek göndermek(yazdýrmak) yerine

onDestroydan önce yazmak mantýklý - onStop 

sharedpreferences kaybetmemek için uygulama döngüsünde = local database